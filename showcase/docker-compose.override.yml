version: '2'

services:
 rabbitmq:
  ports:
   - 15673:15672
 mysql:
  volumes:
   - showcase_mysql:/var/lib/mysql
 phpmyadmin:
  environment:
   - MYSQL_ROOT_PASSWORD:my-secret-pw
  ports:
   - 81:80
 phreeze:
  ports:
   - 82:80

volumes:
 showcase_mysql:
