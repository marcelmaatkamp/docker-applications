#!/bin/bash
shopt -s expand_aliases

server=stein
alias dc="docker-compose -f docker-compose.yml -f docker-compose/docker-compose-$server.yml"
dc stop && dc rm -f --all 
git fetch --all
git reset --hard origin/master
dc build && dc up -d
